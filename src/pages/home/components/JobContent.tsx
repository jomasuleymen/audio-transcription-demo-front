import { Typography } from 'antd';
import React from 'react';

type Props = {};

const JobContent: React.FC<Props> = ({}) => {
	return <Typography.Text>JobContent</Typography.Text>;
};

export default JobContent;
